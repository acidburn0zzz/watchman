#!/bin/env watchman
# A slightly more complicated service file with function overrides

service_command='/bin/agetty'
service_args="38400 ${sname##*-} linux"

start() {
	watchman.start
	watchman.respawn &
}
